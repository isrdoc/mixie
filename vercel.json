{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "turbo run build --filter=mixie-web && cp -r apps/web/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile"
}